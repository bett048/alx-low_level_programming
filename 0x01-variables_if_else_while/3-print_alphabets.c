#include<stdio.h>
/**
 * main - function
 * Return: Always 0
 */
int main(void)
{
	char n = 97;
	char m = 65;

	while (n <= 122)
		putchar(n);
	while (m <= 90)
		putchar(m);
	putchar("\n");
	return (0);
}
